# require "spec_helper"

# RSpec.describe Main::Views::ApplicationLayout, type: :view do
  # let(:layout)   { Main::Views::ApplicationLayout.new({ format: :html }, "contents") }
  # let(:rendered) { layout.render }

  # it 'contains application name' do
    # expect(rendered).to include('Main')
  # end
# end
